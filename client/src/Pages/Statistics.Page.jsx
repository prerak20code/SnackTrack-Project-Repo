import React from 'react';

export default function StatisticsPage() {
    return (
        <div>
            StatisticsPage - could show sales per item in graphical (gantt)
            chart formats
        </div>
    );
}
