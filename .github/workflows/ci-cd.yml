name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            # Navigate to app directory
            cd /var/www/mern-app

            # Pull latest changes
            git reset --hard
            git pull origin main

            # Install root-level dependencies (optional)
            npm install || true

            ##### 🔧 Kill any process on port 3000 (backend) or 5173 (frontend)
            fuser -k 3000/tcp || true
            fuser -k 5173/tcp || true

            ##### 🔧 Also kill leftover PM2/node processes just in case
            pkill -f "node.*src/server.js" || true
            pkill -f "vite" || true

            # Backend setup
            cd server
            npm install

            # Restart backend with PM2
            pm2 delete snacktrack-backend || true
            pm2 start "npm run dev -- --host" --name snacktrack-backend

            # Frontend setup
            cd ../client
            npm install

            # Restart frontend with PM2 (Vite)
            pm2 delete snacktrack-frontend || true
            pm2 start "npm run dev -- --host --port 5173" --name snacktrack-frontend

            # Save PM2 process list
            pm2 save